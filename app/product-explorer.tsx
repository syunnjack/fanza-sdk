"use client";
import {useState} from "react";import {ProviderHub} from "./provider-hub";
type Item={content_id?:string;title?:string;affiliateURL?:string;date?:string;prices?:{price?:string};iteminfo?:{genre?:Array<{name?:string}>}};
export function ProductExplorer(){const[keyword,setKeyword]=useState("");const[items,setItems]=useState<Item[]>([]);const[message,setMessage]=useState("FANZAは接続済み。他ASPは認証設定後に同じ検索面へ追加されます。");async function search(e:React.FormEvent){e.preventDefault();setMessage("検索中…");try{const r=await fetch(`/api/items?keyword=${encodeURIComponent(keyword)}&hits=9`);const d=await r.json();if(!r.ok)throw new Error();setItems(d.items??[]);setMessage(`${d.totalCount??0}件から表示。取得：${new Date(d.fetchedAt).toLocaleString("ja-JP")}`);}catch{setItems([]);setMessage("公式情報を取得できませんでした。時間を置いてお試しください。");}}return <><ProviderHub/><section className="explorer section" id="products"><div className="sectionHead"><p className="eyebrow">CROSS STORE SEARCH</p><h2>公式情報から横断比較</h2><p>提供元、価格、配信状態、取得時刻を同じ形式で表示。</p></div><form className="searchForm" onSubmit={search}><label>キーワード<input value={keyword} onChange={e=>setKeyword(e.target.value)} maxLength={80} placeholder="作品名・ジャンル"/></label><button>接続済みAPIを検索</button></form><p aria-live="polite" className="searchStatus">{message}</p><div className="productGrid">{items.map((item,i)=><article key={item.content_id??i}><span>FANZA公式データ</span><h3>{item.title??"タイトル情報なし"}</h3><p>{item.iteminfo?.genre?.slice(0,3).map(g=>g.name).filter(Boolean).join(" / ")||"ジャンル未設定"}</p><dl><div><dt>価格</dt><dd>{item.prices?.price??"公式で確認"}</dd></div><div><dt>公開日</dt><dd>{item.date??"―"}</dd></div></dl>{item.affiliateURL&&<a href={item.affiliateURL} rel="sponsored nofollow noopener" target="_blank">FANZAで確認 →</a>}</article>)}</div><p className="affiliate">PR・広告を含みます。価格・配信状況はリンク先で再確認してください。事業者横断時も成果リンクの提供元を明示します。</p></section></>}
